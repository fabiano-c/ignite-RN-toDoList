import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row"
    },

    tagText: {
        fontSize: 14,
        fontWeight: "bold"
    },

    counterText: {
        backgroundColor: "#333333",
        paddingHorizontal: 8,
        paddingVertical: 2,
        borderRadius: 999,
        fontSize: 12,
        fontWeight: "bold",
        color: "#D9D9D9",
        marginLeft: 8,
    },

    purple: {
        color: "#8284FA"
    },

    blue: {
        color: "#4EA8DE"
    }
});